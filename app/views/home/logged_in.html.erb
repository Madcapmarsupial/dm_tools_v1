<!DOCTYPE html>
<html>
  <head>
    <title>DM Tool Suite</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
     <header>
      <h1>DM Tool Suite</h1>
      <p>Hello <%=current_user.email%>:</p>
         <p> Coins: <%=current_user.balance.to_i %></p>
        
    </header>

    <main>
      <section>
        <h2>Welcome to your tool suite!</h2>
        <p>Use our tools to generate a unique and exciting campaign for your next RPG adventure!</p>
          <%= link_to "New Quest", new_quest_url, method: :get%>
          <br><li><form action="<%=quests_url%>" method="get">
            <input type="submit" value="Quest List">
            </form>
          </li>


        <%  quest = current_user.quests.last %>
        <% if quest != nil %>
        <p><%= "pick up where you left off"%></p>
        <%=link_to "#{quest.name}", quest_url(quest.id)  %>
        <%end %>
        <br><%= link_to "Sign out", destroy_user_session_path, method: :delete %>


      </section>
    </main>
    <footer>
    </footer>
  </body>
</html>






<!-- (current_user.id)-->
